# Krotki

# Krotka (tuple) jest uporządkowaną sekwencją elementów taką samą jak lista. Jedyną różnicą jest to,
# że krotki są niemutowalne (niezmienne), podobnie jak typ string. Krotki po utworzeniu nie można modyfikować.
# Krotki tworzymy za pomocą nawiasów (), a wartości są oddzielone przecinkami.

empty_tuple = ()
len(empty_tuple)

num_tuple = (1, 2, 3, 4)
num_tuple[2]